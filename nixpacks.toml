[phases.setup]
nixPkgs = [
  "nodejs_18",
  "yarn-1_x"
]

[phases.install]
cmds = ["yarn install --frozen-lockfile"]

[phases.build]
cmds = ["yarn build"]

[start]
cmd = "serve -s build -l 0.0.0.0:3000"